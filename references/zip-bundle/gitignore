# OS files
.DS_Store
Thumbs.db

# Lock files (LibreOffice/Word temp files)
.~lock.*

# Editor/IDE
*.swp
*.swo
*~
.vscode/
.idea/
.cursor/

# Python
__pycache__/
*.pyc
*.pyo
.ipynb_checkpoints/

# Environment
.env
.env.local
.env.*

# ─── Personal data (do not push) ──────────────────────────────
# These hold your real applicant data and per-application outputs.
# Skill code and templates remain tracked. If you want git history,
# initialize a *private* repo and audit before any public push.

memory.md
base-resumes/
research/
resumes/
scores/
speaking-points/
cover-letters/
applications/
archive/
portfolio/
networking/

# Personal interview-prep artifacts (PDFs, MDs, HTML, per-application PWA outputs).
# The folder is tracked via .gitkeep but its contents are user-data only.
interview-prep/*
!interview-prep/.gitkeep

# Decoded JDs may contain proprietary content from postings.
# Tracked via .gitkeep; .md/.pdf contents excluded.
decoded-jds/*
!decoded-jds/.gitkeep

# Build outputs / temp
node_modules/
*.log
/tmp/

# Local backup files (e.g., .docx.bak created during sanitization passes).
# These often contain pre-redaction PII. Never commit them.
*.bak
*~bak
*.orig
